{
  "name": "ffmpeg-api",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cloudinary": "^2.5.1",
    "express": "^4.19.2",
    "dotenv": "^16.4.5"
    // If 'fetch' is not globally available in your environment (unlikely with Node 20),
    // you might need "node-fetch": "^3.3.2"
  }
}
